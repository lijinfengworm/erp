<?php

/**
 * KllArticleSeoTable
 * 
 * This class has been auto-generated by the Doctrine ORM Framework
 */
class KllArticleSeoTable extends Doctrine_Table
{
    /**
     * Returns an instance of this class.
     *
     * @return object KllArticleSeoTable
     */
    public static function getInstance()
    {
        return Doctrine_Core::getTable('KllArticleSeo');
    }
    //获得w文章的seo
    public function findArticleSeo($bind = []){
        $data = self::getInstance()->createQuery();
        if (!empty($bind['select'])){
            $data->select($bind['select']);
        } else {
            $data->select("*");
        }
        //where 简单判断  如果复杂 建议新写函数
        if(!empty($bind['where']) && count($bind['where']) > 0) {
            foreach($bind['where'] as $k=>$v) {
                $data->addWhere($v);
            }
        }
        //wherein
        if(!empty($bind['wherein'])){
            $data->whereIn('id', $bind['wherein']);
        }
        //limit
        if (!empty($bind['limit'])){
            $data->limit($bind['limit']);
        }
        $data =  $data->fetchArray();
        return $data;
    }    
}